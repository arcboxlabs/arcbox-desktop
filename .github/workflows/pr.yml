name: PR Check

on:
  pull_request:
    branches: [master]

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    name: Build & Test
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.app

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-unknown-linux-musl

      - name: Cache Rust build
        uses: actions/cache@v4
        with:
          path: |
            ../arcbox/target
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Verify protobuf generated code is up-to-date
        run: |
          # Install protoc via Homebrew
          brew install protobuf

          # Generate Swift code from proto definitions
          cd Packages/ArcBoxClient
          ./generate.sh --remote

          # Fail if generated files differ from checked-in versions
          if ! git diff --exit-code Sources/ArcBoxClient/Generated/; then
            echo "::error::Protobuf generated Swift files are out of date. Run 'cd Packages/ArcBoxClient && ./generate.sh' and commit the result."
            exit 1
          fi

      - name: Resolve Swift packages
        run: xcodebuild -resolvePackageDependencies -project ArcBox.xcodeproj -scheme ArcBox

      - name: Build (Debug)
        run: |
          xcodebuild build \
            -project ArcBox.xcodeproj \
            -scheme ArcBox \
            -configuration Debug \
            -destination 'platform=macOS' \
            CODE_SIGN_IDENTITY=- \
            SKIP_RUST_BUILD=1 \
            | xcpretty --color

      - name: Run Tests
        run: |
          xcodebuild test \
            -project ArcBox.xcodeproj \
            -scheme ArcBox \
            -configuration Debug \
            -destination 'platform=macOS' \
            CODE_SIGN_IDENTITY=- \
            SKIP_RUST_BUILD=1 \
            | xcpretty --color
